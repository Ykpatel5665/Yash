// Placeholder for TruthDareSelectScreen
import 'package:flutter/material.dart';

class TruthDareSelectScreen extends StatelessWidget {
  const TruthDareSelectScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Truth or Dare')),
      body: const Center(child: Text('Truth/Dare Select Screen Placeholder')),
    );
  }
}
